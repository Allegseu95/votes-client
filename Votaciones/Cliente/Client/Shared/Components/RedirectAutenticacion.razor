@inject NavigationManager Naviagation

@code {
    protected override void OnInitialized()
    {
        Naviagation.NavigateTo($"authentication/login?returnUrl={Uri.EscapeDataString(Naviagation.Uri)}");
    }
}
